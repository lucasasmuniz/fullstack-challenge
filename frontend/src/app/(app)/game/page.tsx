/** Tela do jogo — implementada no F3 (chart + dead-reckoning + painel inline + apostas ao vivo). */
export default function GamePage() {
  return (
    <main className="flex flex-1 items-center justify-center px-6 text-center">
      <div className="flex flex-col items-center gap-3">
        <p className="tabular text-6xl font-bold text-primary">1.00x</p>
        <p className="text-muted">Tela do jogo — em construção (F3).</p>
      </div>
    </main>
  );
}
