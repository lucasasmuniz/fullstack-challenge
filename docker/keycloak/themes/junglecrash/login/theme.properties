# Login theme do JUNGLEcrash — herda o layout do theme base `keycloak` e sobrepõe só o visual
# (dark + verde neon). styles inclui o CSS base (herdado do parent) + o override.
parent=keycloak
import=common/keycloak
styles=css/login.css css/junglecrash.css
